name: C/C++ CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: flawfinder-action
  # You may pin to the exact commit or the version.
  # uses: deep5050/flawfinder-action@195872df942acc80e2c8cc05912f2cb768a34db3
      uses: deep5050/flawfinder-action@1.0
